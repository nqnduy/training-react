import React, { useEffect, useState } from 'react';
import ComponentCollection from 'components/website/component-collection/ComponentCollection';

const components = (props) => {
    return <ComponentCollection></ComponentCollection>;
};

components.propTypes = {};

export default components;
